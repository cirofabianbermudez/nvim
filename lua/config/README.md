Check if nvim-treesitter is workign with:

```vim
:echo exists("b:ts_highlight")
```

Check the filetype with:

```vim
:set filetype?
```

Check tab expand configuration

```vim
:verbose setlocal expandtab?
```
